1. BufferedInputStream (subclase de InputStream)
// Constructor común
BufferedInputStream(InputStream in)
BufferedInputStream(InputStream in, int size)

// No añade métodos nuevos pero optimiza:
// - read() → Más eficiente por buffer interno
// - skip() → Mejor rendimiento
// - available() → Retorna bytes en buffer + stream subyacente

2. BufferedOutputStream (subclase de OutputStream)
// Constructor
BufferedOutputStream(OutputStream out)
BufferedOutputStream(OutputStream out, int size)

// Método especial
void flush()
// Más importante que en OutputStream base
// Asegura que buffer se vacíe al destino

3. BufferedReader (subclase de Reader)
// Constructor
BufferedReader(Reader in)
BufferedReader(Reader in, int sz)

// MÉTODO CLAVE (muy usado)
String readLine()
// Lee una línea de texto
// Retorna String (sin salto de línea) o null si fin

// Eficiencia mejorada
int read()          // Más rápido por buffer
int read(char[]..)  // Más rápido por buffer

4. BufferedWriter (subclase de Writer)
// Constructor
BufferedWriter(Writer out)
BufferedWriter(Writer out, int sz)

// MÉTODO CLAVE
void newLine()
// Escribe separador de línea específico del sistema
// Mejor que escribir "\n" directamente

5. FileInputStream / FileOutputStream
// Constructores principales
FileInputStream(String name)
FileInputStream(File file)

FileOutputStream(String name)
FileOutputStream(String name, boolean append)  // append = true para añadir
FileOutputStream(File file)
FileOutputStream(File file, boolean append)

// Métodos específicos
FileChannel getChannel()  // Para operaciones NIO

6. FileReader / FileWriter
// Constructores principales
FileReader(String fileName)
FileReader(File file)

FileWriter(String fileName)
FileWriter(String fileName, boolean append)
FileWriter(File file)
FileWriter(File file, boolean append)

7. PrintStream (subclase de OutputStream)
// MÉTODOS MÁS USADOS
void print(boolean b)    // Sobrecargas para todos los tipos
void println(boolean b)  // Igual que print + salto línea

void print(Object obj)   // Usa toString() del objeto
void println(Object obj)

PrintStream printf(String format, Object... args)  // Formato estilo C
PrintStream format(String format, Object... args)  // Igual que printf

// Constructores comunes
PrintStream(OutputStream out)
PrintStream(OutputStream out, boolean autoFlush)
PrintStream(String fileName)  // Java 5+

8. PrintWriter (subclase de Writer)
// MÉTODOS SIMILARES A PrintStream
void print(boolean b)
void println(boolean b)
void print(Object obj)
void println(Object obj)
PrintWriter printf(String format, Object... args)
PrintWriter format(String format, Object... args)

// Constructores comunes
PrintWriter(Writer out)
PrintWriter(Writer out, boolean autoFlush)
PrintWriter(OutputStream out)  // Convierte bytes a caracteres
PrintWriter(OutputStream out, boolean autoFlush)
PrintWriter(String fileName)   // Java 5+

9. DataInputStream (subclase de FilterInputStream)
// MÉTODOS PARA TIPOS PRIMITIVOS
byte readByte()
short readShort()
int readInt()
long readLong()
float readFloat()
double readDouble()
char readChar()
boolean readBoolean()

// Para lectura de texto con codificación UTF
String readUTF()  // Lee String en formato UTF modificado

// Para leer arrays completos
void readFully(byte[] b)      // Lee hasta llenar el array
void readFully(byte[] b, int off, int len)

10. DataOutputStream (subclase de FilterOutputStream)
// MÉTODOS PARA TIPOS PRIMITIVOS
void writeByte(int v)
void writeShort(int v)
void writeInt(int v)
void writeLong(long v)
void writeFloat(float v)
void writeDouble(double v)
void writeChar(int v)
void writeBoolean(boolean v)

// Para escritura de texto
void writeUTF(String str)  // Escribe String en formato UTF modificado

// Para control de escritura
int size()  // Retorna número de bytes escritos

11. InputStreamReader / OutputStreamWriter (puentes bytes↔caracteres)
// Constructores (IMPORTANTE: especificar charset)
InputStreamReader(InputStream in)
InputStreamReader(InputStream in, Charset cs)      // Recomendado
InputStreamReader(InputStream in, CharsetDecoder dec)
InputStreamReader(InputStream in, String charsetName)

OutputStreamWriter(OutputStream out)
OutputStreamWriter(OutputStream out, Charset cs)   // Recomendado
OutputStreamWriter(OutputStream out, CharsetEncoder enc)
OutputStreamWriter(OutputStream out, String charsetName)

// Métodos para obtener charset
Charset getEncoding()  // Retorna charset usado

12. RandomAccessFile (NO es subclase de InputStream/OutputStream)
// Implementa DataInput y DataOutput
// Constructor
RandomAccessFile(String name, String mode)  // mode: "r", "rw", "rws", "rwd"

// MÉTODOS CLAVE
void seek(long pos)           // Mueve puntero a posición específica
long getFilePointer()         // Retorna posición actual
long length()                 // Retorna tamaño del archivo
void setLength(long newLength)// Cambia tamaño del archivo

// Tiene todos los métodos readXXX() y writeXXX() de DataInput/DataOutput

